FRAMEWORK_GIT_URL=https://github.com/your-org/pw-api-framework.git
FRAMEWORK_BRANCH=main
FRAMEWORK_WORKSPACE_DIR=./workspace/framework
FRAMEWORK_GIT_TOKEN=
FRAMEWORK_REFRESH_ON_START=false

PORT=3000
HOST=0.0.0.0

# --- LLM (steps 5g + 12 only; rest of pipeline stays deterministic) ---
# Pick provider: gemini | openai | heuristic
LLM_PROVIDER=gemini

# Paste your Gemini key here (https://aistudio.google.com/apikey)
GEMINI_API_KEY=
# Quotas are per model — pick one that still has free-tier headroom
GEMINI_MODEL=gemini-2.5-flash-lite
# Auto-retry these on 429 (separate quotas from primary)
GEMINI_MODEL_FALLBACKS=gemini-2.5-flash,gemini-1.5-flash,gemini-2.0-flash

# Continue pipeline when LLM quota/rate-limit fails (default: true)
LLM_FAIL_OPEN=true

# Single generic LLM prompt: agent-knowledge/prompts/llm.system.txt
# LLM_PROMPTS_DIR=./agent-knowledge/prompts

# Or use OpenAI instead — set LLM_PROVIDER=openai and paste key below
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
